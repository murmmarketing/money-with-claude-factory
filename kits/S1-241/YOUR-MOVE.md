# YOUR MOVE — human-only steps (≈40 min)

1. **Create the paid product:** upload `vault/freelance-web-dev-vault.md` to Gumroad (or Stripe
   Payment Link + file delivery) as "The Freelance Web Dev Vault — $29". Copy the checkout URL.
2. **Create the free lead magnet:** upload `vault/free-teaser-5-prompts.md` as a $0 / email-gated
   download (Gumroad supports "pay what you want / free"). Copy that opt-in URL.
3. **Wire the storefront:** in `index.html`, replace every `REPLACE_CHECKOUT_URL` with your paid
   link and every `REPLACE_FREE_OPTIN_URL` with your free opt-in link. (Also swap them into the two
   `.md` teaser/landing files.)
4. **Deploy the page:** drag `index.html` into Vercel/Netlify, or add it as a `/vault` route on
   murmweb.dev. Confirm it opens and the buttons work.
5. **Launch:** post the 3 pieces in `marketing.md` and send the free teaser to your murmweb.dev list.
6. **Approve pricing:** $29 launch (reg. $39). Decide if/when to add the $79 Pro Vault upsell (SOP
   docs + email templates + a Notion client-portal template).

That's it — zero fulfillment after setup. Every sale is pure margin. When it works, reskin for the
next niche using the same files.
